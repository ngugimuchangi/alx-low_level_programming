#include "main.h"
/**
 * _islower - check if lower case
 * @c: character to check
 * Return: 1 equal success, 0 equals failure
 */
int _islower(int c)
{
	if (c => 'a' && c <= 'z')
	{
	return (1);
	}
	else
	{
	return (0);
	}

}
